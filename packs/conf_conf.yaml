http:

mobile_app:

# cast:
#   media_player:
#     - uuid: !secret cast_bedroom_id
#     - uuid: !secret cast_sound_bar_id

# ota update trigger - ikea
# issue_zigbee_cluster_command
# ieee: 'd0:cf:5e:ff:fe:14:6d:c6'
# endpoint_id: 1
# cluster_type: out
# command_type: client
# cluster_id: 25
# command: 0
# args:
#   - 0
#   - 100

zha:
  zigpy_config:
    ota:
      ikea_provider: true
  database_path: /config/zigbee.db

system_log:
  max_entries: 20

logger:
  default: warn
  # logs:
  #   homeassistant.core: debug
  #   zigpy: debug
